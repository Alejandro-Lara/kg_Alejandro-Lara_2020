# kg_Alejandro-Lara_2020

For this assessment, I'm assuming that the "one-to-one" relationships are outlined by characters in the same "index" between the two strings.

Following the example in the document: (bar foo) -> true ; different characters in the first string may map to the same character in the second string ('a' and 'r' going to 'o'), so long as they are consistant about that in future appearances in the string
